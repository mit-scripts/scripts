---
- name: Install scripts-httpd
  dnf:
    name:
      - scripts-httpd
      - scripts-mod_ssl
      - mod_ldap
      - httpdmods
    state: present
- name: Configure httpd
  copy:
    dest: /etc/httpd/
    src: httpd/
  notify: reload httpd
- name: Enable httpd
  service:
    name: httpd
    enabled: yes
    state: started
