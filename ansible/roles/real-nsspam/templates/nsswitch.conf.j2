passwd:     files {% if use_nss_nonlocal %}nonlocal
passwd_nonlocal: {% endif %}ldap
shadow:     files
group:      files {% if use_nss_nonlocal %}nonlocal
group_nonlocal: {% endif %}ldap
hosts:      files dns myhostname

ethers:     files
netmasks:   files
networks:   files
protocols:  files
rpc:        files
services:   files

bootparams: files
automount:  files
aliases:    files
